#include<bits/stdc++.h>
#include<stack>
#include<vector>
using namespace std;


//time complexity of stack is O(1)  
inline void explainstack(){
    stack<int> st;
    st.push(1);
    st.push(2);
    st.push(3);
    st.push(4);
    st.emplace(5);
   
    cout<<st.top()<<endl;
    st.pop();
    cout<<st.top()<<endl;
    cout<<st.empty()<<endl;
    stack<int>st1 , st2;
    st1.swap(st2);
}
int main(){
    
    explainstack();
    return 0;
}
